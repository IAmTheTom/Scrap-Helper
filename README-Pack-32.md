# Pack 32 — Local Model Integration Path

Adds local model-pack import/validation UI and a numeric-output adapter.

No production Scrap Helper model is bundled. This pack prepares the app to import the first valid `.tflite` model pack locally.
