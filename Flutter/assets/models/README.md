# Scrap Helper local model assets

No trained model is included in Pack 30.

A model pack contains:

- `manifest.json`
- a `.tflite` model
- a label text file

The setup validates the runtime bridge and real pixel preprocessing without pretending that a production recognition model already exists.

The first trained model can later be bundled here or imported through the model-pack installer.
