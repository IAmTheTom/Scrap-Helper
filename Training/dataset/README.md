# Scrap Helper training dataset

Place source photos in `images/` and one JSON annotation per image in `annotations/`.
Bounding boxes use normalized `[left, top, width, height]` values from 0 to 1.
Keep a held-out test set that is never used during training.
